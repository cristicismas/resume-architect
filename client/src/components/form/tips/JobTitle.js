import React from 'react';

const JobTitle = () => (
  <section id="tip">
    <h2 className="title">JobTitle</h2>

    <p className="text">Your JobTitle.</p>
  </section>
);

export default JobTitle;
